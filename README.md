# metanet-calibration

1. Clone repository
2. initialize virtual environment 
3. Navigate to repository folder
4. Run `pip install -e .` to install this repository as a package.
5. Example notebook in  `examples/` can be run to test calibration. There will be machine to machine differences in calibration results due to the nature of IPOPT.